# من مؤشر Pine Script إلى منصة تحليل ويب — التوثيق الفني الكامل

> مبني على التحليل الدقيق لملف `مؤشر_المثلثات_20026.txt` (767 سطر)، مطابَق فعليًا مع أرقام الصورة المرفقة.

---

## 1. الخوارزمية بصيغة Pseudocode (جاهزة للترجمة إلى Python/TypeScript)

### 1.1 المدخلات والحسابات الأساسية

```
INPUT: candles[] = [{time, open, high, low, close, volume}, ...]   // ترتيب زمني تصاعدي

PARAMS:
    lbL = 5, lbR = 5, rangeLower = 5, rangeUpper = 60, rsiLen = 14
    trendLen = 50, atrLen = 14, atrMult = 2.0
    structLen = 8          // طول نافذة القمة/القاع لمثلث الهيكل الرئيسي
    subStructLen = 3        // للمثلث الفرعي (عرض بصري فقط)
    ema50Len = 50, ema200Len = 200
    volMaLen = 20, volMaThreshold = 0.8
    mtfList = [1,3,5,15,30,45,60,75,120,240,"D"]   // بالدقائق أو "D"=يومي

DERIVED_SERIES:
    rsi        = RSI(close, rsiLen)                 // Wilder smoothing
    atr        = ATR(high, low, close, atrLen)       // Wilder smoothing
    trendMA    = SMA(close, trendLen)
    ema50      = EMA(close, ema50Len)
    ema200     = EMA(close, ema200Len)
    volMa      = SMA(volume, volMaLen)
    isUptrend  = close[i] > trendMA[i]
    isVolOk    = volume[i] > volMa[i] * volMaThreshold
```

### 1.2 كاشف القمة/القاع (Pivot) — أساس كل المحركات

```
FUNCTION pivotHigh(series, left, right, i):
    // نقطة ارتكاز مؤكدة فقط بعد مرور "right" شمعة (lag متعمد، هذا هو سلوك TradingView الحقيقي)
    IF i < left + right: RETURN None
    center = series[i - right]
    IF center == MAX(series[i-left-right : i+1]):
        RETURN center
    RETURN None

FUNCTION pivotLow(series, left, right, i):
    // نفس المنطق لكن MIN
    ...
```

⚠️ **نقطة حرجة للمطابقة مع TradingView:** أي Pivot لا يُعرف إلا بعد مرور `right` شمعة من ظهوره — أي القيمة "الحالية" في آخر شمعة لا يمكن اعتبارها Pivot أبدًا حتى تمر عليها 5 أو 8 شموع (حسب المحرك). هذا يعني أن **آخر قمة/قاع معروضين في الواجهة سيكونان دائمًا متأخرين بعدد شموع = right** عن آخر سعر — تمامًا كما في TradingView الأصلي. أي تطبيق يُظهر Pivot "لحظي" غير مؤخّر لن يطابق النتيجة الأصلية.

### 1.3 محرك التزامن بين RSI والسعر (المُسمّى "ديفرجنس" في الكود الأصلي)

> **تنويه مهم:** كما أوضحت في التحليل السابق، الشرط الفعلي المُبرمج هو **تزامن اتجاه RSI مع السعر** (كلاهما يتحرك بنفس الاتجاه)، وليس التباين الكلاسيكي (اتجاهان متعاكسان). سأحافظ على هذا المنطق **حرفيًا** لضمان مطابقة الإشارات، لكن سأسميه في النظام الجديد "تأكيد الزخم عند نقطة الارتكاز" ليكون الاسم صادقًا مع ما يحسبه فعليًا، مع إبقاء الاسم القديم كمرادف تجاري إن رغبت.

```
FOR each confirmed pivot low at index i (بعد lbR شمعة من ظهوره):
    prevPL = آخر pivot low قبله (index j < i)
    priceHigherLow = low[i] > low[j]
    rsiHigherLow   = rsi[i] > rsi[j]
    barsBetween    = i - j
    inRange        = rangeLower <= barsBetween <= rangeUpper

    IF priceHigherLow AND rsiHigherLow AND inRange:
        signal = "HIDDEN_BULL"   // "كسر صاعد"
        entry  = close[i]
        atrAtSignal = atr[i]
        target = entry + atrAtSignal * atrMult
        stop   = entry - atrAtSignal * atrMult
        midPrice = (target + stop) / 2   // == entry رياضيًا

FOR each confirmed pivot high at index i:
    priceLowerHigh = high[i] < high[j]
    rsiLowerHigh   = rsi[i] < rsi[j]
    IF priceLowerHigh AND rsiLowerHigh AND inRange:
        signal = "HIDDEN_BEAR"   // "كسر هابط"
        entry  = close[i]
        target = entry - atr[i] * atrMult
        stop   = entry + atr[i] * atrMult
```

### 1.4 محرك المثلث/الهيكل الرئيسي (Golden Triangle — المصدر الحقيقي للاتجاه)

```
FUNCTION calcStructure(candles, len, currentIndex):
    ph1, ph1_x = آخر pivot high مؤكد (قيمة، موقعه)
    ph2, ph2_x = الـ pivot high الذي قبله
    pl1, pl1_x = آخر pivot low مؤكد
    pl2, pl2_x = الـ pivot low الذي قبله

    IF ph2 is None OR pl2 is None:
        RETURN {state: 0, upper: None, lower: None, height: None}   // بيانات غير كافية بعد

    m1 = (ph1 - ph2) / max(1, ph1_x - ph2_x)     // ميل خط المقاومة
    m2 = (pl1 - pl2) / max(1, pl1_x - pl2_x)     // ميل خط الدعم

    upperNow = m1 * (currentIndex - ph1_x) + ph1
    lowerNow = m2 * (currentIndex - pl1_x) + pl1
    height   = abs(ph1 - pl1)

    closeNow = candles[currentIndex].close
    IF closeNow > upperNow:      state = 1    // اختراق صاعد 🔥
    ELIF closeNow < lowerNow:    state = -1   // كسر هابط 🚨
    ELSE:                        state = 0    // داخل المثلث ⏳

    RETURN {state, upper: upperNow, lower: lowerNow, height,
            resistanceLine: (ph2_x,ph2)→(ph1_x,ph1) ممتد بالميل m1,
            supportLine:    (pl2_x,pl2)→(pl1_x,pl1) ممتد بالميل m2}
```

**قوة الاتجاه العامة (منفصلة عن الحالة أعلاه):**
```
IF close > ema50 AND ema50 > ema200:   trendStrength = "STRONG_BULL"   // صاعد قاسي
ELIF close < ema50 AND ema50 < ema200: trendStrength = "STRONG_BEAR"   // هابط قاسي
ELSE:                                   trendStrength = "CHOPPY"        // تذبذب
```

### 1.5 محرك الأهداف الذكية (Measured Move)

```
STATE: activeTargetDirection = 0   // يُحفظ بين الاستدعاءات (مثل Pine Script var)

realBreakout  = close > structure.upper
realBreakdown = close < structure.lower

IF activeTargetDirection != 0 AND priceCrossed(close, target1Price):
    activeTargetDirection = 0   // إلغاء الهدف، البحث عن هدف جديد

IF realBreakout AND activeTargetDirection != 1
   AND rsi > 45 AND isVolOk AND close > ema50:
        target1 = close + structure.height * 0.5
        target2 = close + structure.height * 1.0
        activeTargetDirection = 1

IF realBreakdown AND activeTargetDirection != -1
   AND rsi < 55 AND isVolOk AND close < ema50:
        target1 = close - structure.height * 0.5
        target2 = close - structure.height * 1.0
        activeTargetDirection = -1
```

### 1.6 رادار الفريمات المتعددة

```
FOR each tf IN [1m,3m,5m,15m,30m,45m,1h,75m,2h,4h,1D]:
    candlesTF = fetchOrResampleCandles(symbol, tf)
    result[tf] = calcStructure(candlesTF, structLen=8, lastIndex).state
    // -1 = كسر🚨 | 0 = داخل⏳ | 1 = اختراق🔥
```

### 1.7 دمج كل شيء في مخرج تحليل واحد (هذا هو الـ API Response الجوهري)

```
OUTPUT = {
    trend:            structure.state == 1 ? "BULLISH" : structure.state == -1 ? "BEARISH" : "NEUTRAL"
    trendStrength:     f(txt_trend)  // STRONG / CHOPPY حسب EMA50 مقابل EMA200
    signalStrength:    دالة مركّبة (انظر قسم "التحسينات" لتفاصيل confidence score)
    support:           structure.lower
    resistance:        structure.upper
    midpoint:          (structure.upper + structure.lower) / 2
    lastPivotHigh:     {price: ph1, time: candles[ph1_x].time}
    lastPivotLow:      {price: pl1, time: candles[pl1_x].time}
    bullishTargets:    [target1_up, target2_up]  // إن وُجدت
    bearishTargets:    [target1_down, target2_down]
    lastHiddenSignal:  {type, price, time, target, stop, midPrice}
    mtfRadar:          {1m: state, 3m: state, ... 1D: state}
    reasoning:         نص تفسيري (انظر 1.8)
}
```

### 1.8 توليد "سبب التحليل" (Reasoning) آليًا — وهذا ما طلبته صراحة

```
reasoning = []

IF structure.state == 1:
    reasoning.append("السعر أغلق فوق خط المقاومة الديناميكي المرسوم من آخر قمتين "
                      "(" + ph2 + " → " + ph1 + ")، مما يعني اختراقًا فعليًا للهيكل الحالي.")
IF rsi > 45 (في حالة الصعود):
    reasoning.append("مؤشر RSI عند " + rsi + " وهو أعلى من عتبة 45 المطلوبة لتأكيد الزخم الصاعد.")
IF isVolOk:
    reasoning.append("حجم التداول الحالي أعلى من 80% من متوسطه لـ20 شمعة، أي سيولة كافية لدعم الحركة.")
IF close > ema50:
    reasoning.append("السعر فوق المتوسط EMA50 مما يدعم استمرارية الاتجاه الصاعد.")
IF hiddenBullCond triggered:
    reasoning.append("عند آخر قاع سعري، تحرك RSI والسعر معًا نحو الأعلى (قاع أعلى في كليهما) "
                      "خلال نطاق " + barsBetween + " شمعة، وهو ما يعتبره النظام تأكيدًا للاستمرار الصاعد.")
IF structure.state == 0:
    reasoning.append("السعر لا يزال داخل حدود المثلث بين الدعم عند " + lower + " والمقاومة عند "
                      + upper + "، لا توجد إشارة اتجاه حاسمة بعد.")

RETURN join(reasoning, " ")
```

هذا يجعل كل نتيجة على الموقع "قابلة للتفسير" حرفيًا من نفس الشروط البرمجية التي أنتجتها — لا تخمين ولا صياغة عامة.

---

## 2. بنية النظام التقنية المقترحة

```
┌─────────────────────────────────────────────────────────────┐
│  Frontend — Next.js 14 (React + TypeScript)                  │
│  • TradingView Lightweight Charts (نفس مكتبة TV الرسمية،      │
│    مجانية، تعطي شكل الشموع/الخطوط مطابقًا بصريًا)              │
│  • Zustand/React Query لإدارة حالة الطلبات                    │
│  • Tailwind CSS لواجهة داكنة شبيهة بمنصات التداول             │
└───────────────────────┬─────────────────────────────────────┘
                         │ REST /analyze?symbol=&tf=   (JSON)
                         │ WebSocket /stream (أسعار لحظية اختياري)
┌───────────────────────▼─────────────────────────────────────┐
│  Backend — Python / FastAPI                                  │
│  • محرك التحليل (numpy + pandas): إعادة بناء دقيقة لكل         │
│    الحسابات في القسم 1 (Pivot/RSI/ATR/EMA بنفس صيغ Wilder)    │
│  • طبقة تجريد لمزوّد بيانات الأسعار (provider-agnostic)        │
│  • Redis: تخزين مؤقت لنتائج التحليل (TTL = مدة الشمعة)         │
│  • Celery/Cron (اختياري): تحديث دوري لرمز نشط بدل حساب لحظي   │
└───────────────────────┬─────────────────────────────────────┘
                         │
┌───────────────────────▼─────────────────────────────────────┐
│  مزوّد بيانات الأسعار (يُختار حسب فئة الأصل):                  │
│  • أسهم/فوركس: Twelve Data / Finnhub / Alpaca / Polygon.io    │
│  • عملات رقمية: Binance API (مجاني وسريع جدًا للشموع)         │
│  ⚠ يجب التحقق من الشروط الحالية لكل مزوّد وقت التنفيذ الفعلي،  │
│    فالخطط والأسعار والحدود تتغيّر باستمرار.                    │
└─────────────────────────────────────────────────────────────┘
                         │
┌───────────────────────▼─────────────────────────────────────┐
│  PostgreSQL (+ TimescaleDB extension)                         │
│  • تخزين الشموع المخزّنة مؤقتًا لتقليل نداءات API              │
│  • تخزين لقطات التحليل التاريخية (لبناء سجل أداء الإشارات)     │
└─────────────────────────────────────────────────────────────┘
```

### لماذا هذا الاختيار تحديدًا؟

- **Lightweight Charts** بدل Chart.js/Recharts: لأنها **نفس** مكتبة TradingView مفتوحة المصدر، فتقارب الشكل البصري (الشموع، الخطوط الممتدة، التظليل بين خطين) تلقائي دون إعادة اختراع، وتدعم `createPriceLine` و`LineSeries` بسهولة لرسم الدعم/المقاومة والأهداف بنفس أسلوب الصورة المرفقة.
- **Python/FastAPI** للـ backend: لأن `pandas`/`numpy` توفّران دوال RSI/ATR/EMA جاهزة الأداء، وإعادة كتابة منطق Pine Script (خصوصًا حلقات `valuewhen` و`barssince`) أوضح وأسرع تطويرًا بـ pandas من TypeScript الخام. إن كان فريقك يفضّل لغة واحدة فقط (TS في كل مكان)، البديل هو Node.js + `technicalindicators` npm package — لكن دقة Wilder smoothing يجب التحقق منها يدويًا لأنها أقل نضجًا من pandas-ta.
- **Redis** ضروري لأن حساب Pivot لسلاسل طويلة على كل طلب مكلف زمنيًا؛ التخزين المؤقت بمدة = مدة الشمعة (60 ثانية لفريم 1m، 300 لفريم 5m...) يمنع إعادة الحساب لنفس الرمز والفريم من كل مستخدم.
- **TimescaleDB** بدل PostgreSQL عادي: لأنها مصممة خصيصًا لبيانات OHLCV الزمنية (ضغط تلقائي + استعلامات نطاق زمني سريعة جدًا).

---

## 3. مخطط قاعدة البيانات (مبدئي)

```sql
-- الرموز المدعومة
symbols (
    id, ticker, display_name, asset_class ENUM('stock','crypto','forex'),
    provider_symbol, created_at
)

-- تخزين الشموع (Hypertable في TimescaleDB)
ohlcv_cache (
    symbol_id, timeframe, ts, open, high, low, close, volume,
    PRIMARY KEY (symbol_id, timeframe, ts)
)

-- لقطة كل تحليل تم إنتاجه (لبناء سجل أداء وConfidence مستقبلي)
analysis_snapshots (
    id, symbol_id, timeframe, ts,
    trend, trend_strength, structure_state,
    support, resistance, midpoint,
    last_pivot_high_price, last_pivot_high_ts,
    last_pivot_low_price,  last_pivot_low_ts,
    target1_up, target2_up, target1_down, target2_down,
    hidden_signal_type, hidden_signal_price, hidden_signal_ts,
    confidence_score, reasoning_text
)

-- اختياري: تنبيهات المستخدم
alerts (
    id, user_id, symbol_id, timeframe, condition, is_active, created_at
)

-- اختياري: تتبع أداء الإشارات فعليًا (لقياس دقة النظام بمرور الوقت)
signal_outcomes (
    snapshot_id REFERENCES analysis_snapshots(id),
    hit_target1 BOOL, hit_target2 BOOL, hit_stop BOOL, resolved_at
)
```

---

## 4. تصميم واجهة الموقع

**الصفحة الرئيسية (تخطيط من ثلاثة أعمدة):**

1. **الشريط العلوي:** مربع بحث (autocomplete للرموز) + قائمة اختيار الفريم (1m/3m/5m/15m/1h...) + زر "تحليل" بارز.
2. **العمود الأيسر — لوحة النتائج:**
   - شارة كبيرة ملوّنة: 🟢 Bullish (Call) / 🔴 Bearish (Put)، مع شريط قوة (Weak/Moderate/Strong).
   - بطاقة Confidence Score برقم مئوي وشريط تقدّم دائري.
   - قائمة: الدعم / المقاومة / المنتصف السعري / آخر قمة / آخر قاع / الأهداف الصاعدة / الأهداف الهابطة.
   - صندوق "سبب التحليل" — نص reasoning المولّد آليًا (قسم 1.8) بنقاط قابلة للطي.
   - جدول رادار الفريمات (11 صفًا) بنفس أيقونات 🔥/🚨/⏳ الأصلية.
3. **العمود الأوسط/الأيمن — الرسم البياني التفاعلي:**
   - شموع + EMA50 (أخضر) + EMA200 (أزرق).
   - خطا المقاومة/الدعم الديناميكيان (نفس ميل المثلث) ممتدان يمينًا.
   - تظليل شفاف بين الخطين بلون يعكس `close > EMA50`.
   - خطوط منقطة أفقية للأهداف والوقف، وخط أبيض منقط للمنتصف — تمامًا كصورتك.
   - تسميات (labels) عند نقاط "كسر صاعد/هابط".

**نمط بصري:** خلفية داكنة (#0a0a0a إلى #111111)، أخضر تركوازي `#089981` للصعود، أحمر `#f23645` للهبوط — نفس لوحة الألوان الموجودة فعليًا في الكود الأصلي، فهذا يضمن اتساقًا بصريًا كاملًا مع ما اعتاده المستخدم في TradingView.

---

## 5. خطة التنفيذ خطوة بخطوة

| المرحلة | المهام | الناتج |
|---|---|---|
| **1. الأساس** | إعداد FastAPI + Next.js + PostgreSQL/TimescaleDB + Redis محليًا | هيكل مشروع جاهز |
| **2. طبقة البيانات** | ربط مزوّد بيانات واحد (ابدأ بـ Binance للعملات لأنه مجاني وبلا مفتاح للبيانات العامة)، تخزين الشموع في `ohlcv_cache` | إمكانية جلب OHLCV لأي رمز مدعوم |
| **3. محرك التحليل** | ترجمة القسم 1 كاملًا إلى Python (pivot, RSI, ATR, EMA, structure, targets)، مع اختبارات وحدة تقارن المخرجات يدويًا مع القيم الظاهرة في TradingView على نفس الشمعة | دالة `analyze(symbol, timeframe) -> AnalysisResult` موثوقة |
| **4. API** | نقطة `/analyze` تُرجع الـ JSON الموصوف في 1.7، مع تخزين مؤقت Redis | API جاهز للاستهلاك |
| **5. الواجهة الأساسية** | البحث + اختيار الفريم + لوحة النتائج النصية (بدون رسم بعد) | MVP وظيفي |
| **6. الرسم البياني** | دمج Lightweight Charts، رسم الشموع + الخطوط + الأهداف + الألوان | تطابق بصري مع TradingView |
| **7. رادار الفريمات** | استدعاء `analyze` لكل الفريمات الـ11 (بالتوازي async) وعرضها كجدول | لوحة الرادار الكاملة |
| **8. التحقق والمطابقة** | فتح نفس الرمز/الفريم في TradingView الأصلي جنبًا إلى جنب مع الموقع، ومقارنة كل رقم (دعم/مقاومة/هدف/RSI) لعدة عشرات من الشموع تاريخيًا | تقرير دقة/فروق |
| **9. تحسينات** | Confidence Score، تنبيهات، سجل أداء الإشارات (انظر القسم 6) | نظام ناضج |
| **10. نشر** | Docker Compose (frontend/backend/db/redis) → نشر على استضافة (Render/Railway/VPS) | إصدار مباشر |

**كيف تُطابق النتائج TradingView بأعلى نسبة ممكنة؟**
- استخدم **نفس صيغة Wilder** لـ RSI/ATR (وليس EMA بسيطة) — هذا أكبر مصدر فروق شائع بين المؤشرات المُعاد بناؤها.
- تأكد أن الشمعة الأخيرة **غير المكتملة** (still forming) تُعامل بنفس منطق `barstate.islast` — أي التحديث اللحظي يُعاد حسابه على كل تحديث سعر، بينما القمم/القيعان لا تُعتبر "مؤكدة" إلا بعد `lbR`/`length` شمعة كما ذكرنا في 1.2.
- استخدم بيانات من نفس مصدر السعر الذي يستخدمه TradingView للرمز نفسه إن أمكن (فروق بسيطة بين المزوّدين في السعر اللحظي أمر طبيعي وغير قابل للإلغاء الكامل).
- طابق المنطقة الزمنية (الكود الأصلي يستخدم `Asia/Riyadh` في تنسيق الوقت).

---

## 6. تحسينات مقترحة تزيد ذكاء النظام دون تغيير منطقه الأساسي

هذه إضافات **حول** المنطق الأصلي، لا تعديل عليه — يبقى المحرك الأساسي كما هو تمامًا:

1. **Confidence Score مركّب** (رقم واحد 0-100%) من دمج:
   - حالة الهيكل الرئيسي (وزن أكبر)
   - عدد الفريمات في الرادار المتفقة مع نفس الاتجاه (كلما زاد الاتفاق زادت الثقة)
   - موقع RSI من عتبتي 45/55
   - `isVolOk` كعامل ثنائي (يخفض الثقة إن كانت السيولة ضعيفة)
   - محاذاة EMA50/EMA200 (قوة الاتجاه العامة)
   
   *كل هذه القيم موجودة أصلًا في الكود — فقط تُدمج برقم واحد بدل عرضها متفرقة.*

2. **سجل أداء تاريخي (Backtestable):** بما أن `analysis_snapshots` و`signal_outcomes` تُخزَّنان، يمكن بناء تقرير "نسبة نجاح هذا النوع من الإشارات تاريخيًا على هذا الرمز/الفريم" — وهذا يحوّل "نسبة النجاح المتوقعة" من تخمين إلى رقم مبني على بيانات فعلية.

3. **تنبيهات ذكية:** عند تغيّر `structure_state` أو ظهور `hidden signal` جديد، إرسال إشعار (Webhook/Telegram/Email) بدل انتظار المستخدم لفتح الموقع.

4. **مقارنة الفريمات تلقائيًا:** بدل عرض الرادار فقط كجدول، حساب "درجة اتساق الاتجاه عبر الفريمات" (Multi-Timeframe Alignment Score) كمؤشر منفصل يُبرز عندما تتفق أغلب الفريمات — وهذا يقوّي جودة القرار دون المساس بمعادلة كل فريم بمفرده.

5. **Walk-forward validation دوري:** تشغيل تلقائي أسبوعي يقارن مخرجات محرك التحليل الخاص بك بمخرجات TradingView الفعلية لعينة من الرموز، لرصد أي انحراف حسابي مبكرًا.

---

**الخطوة التالية المنطقية:** كتابة الكود الفعلي لمحرك التحليل (القسم 1) بلغة Python كاملًا وقابلًا للاختبار، ثم نقطة الـ API. أخبرني إن أردت البدء بذلك الآن، أو تفضّل مراجعة أي قسم من هذا التوثيق أولًا.
